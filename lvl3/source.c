int m;

int v() 
{
    char str[520];

    fgets(str, len, stdin)
    printf(str)
    if (m == 0x40)
    {
        fwrite("Wait what?!");
        system("/bin/sh");
    }
    return (0);
}

int main (int ac, char** av) 
{
    v();
    return (0);
}
