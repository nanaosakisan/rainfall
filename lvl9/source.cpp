classe N {
    int nb;
    char str[108];

    void N(number)
    {
        this->nb = number;
    }
    
    int N_operator+(number)
    {
        return this->nb + number;
    }

    int N_operator-(number)
    {
        return this->nb - number;
    }

    void setAnnotation(char* s) {
        len = strlen(c)
        memcpy(this->str, s, len);
    }
}

int main (int ac, char **av)
{
    N *n1, *n2;

    if (ac != 2)
        exit();

    n1 = new N(5);

    n2 = new N(6);
    
    N1.setAnnotation(av[1]);

    return *n1 = *n1 + *n2
}